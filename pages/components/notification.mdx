import {
  VKNotifications,
  useVKNotifications,
  Button,
} from "@vivakits/react-components";
import { Tabs } from "nextra/components";

## Notification

The Notification component is a versatile and customizable UI element designed to display brief, important messages to users. These notifications can serve various purposes, such as providing feedback on user actions, displaying system messages, or alerting users to important information. The component offers a wide range of properties to tailor its appearance and behavior, ensuring it can fit seamlessly into any application's design.

### Import

To utilize the Notification prop, import it from `@vivakits/react-components`

```jsx copy
import {
  VKNotifications,
  useVKNotifications,
} from "@vivakits/react-components";
```

### Variant

The `variant` prop specifies the variant of the notification, which can dictate its appearance and style. The Notification component comes with 4 different variants, `default` `success` `warning` `error`, each with its own color scheme and icon, that can be changed using the `variant` prop inside the `notify` function.

<Tabs items={['Preview', 'Code']}>

<Tabs.Tab>
export const NotificationsVariantGroup = () => {
    const { notify } = useVKNotifications();
  return (
    <>
        <VKNotifications />
        <div className="flex items-center gap-3">
            <Button
                size="md"
                rounded="lg"
               onClick={() => {
                    notify({
                     variant: "default",
                        title: "Default",
                        description: "Default Notification",
                        autoClose: true,
                        duration: 3000
                    });
                }}
            >
                Default
            </Button>

            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        variant: "success",
                        title: "Success",
                        description: "Success Notification",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
                Success
            </Button>

            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        variant: "warning",
                        title: "Warning",
                        description: "Warning Notification",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
                Warning
            </Button>

            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        variant: "error",
                        title: "Error",
                        description: "Error Notification",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
                Error
            </Button>
        </div>
    </>

);
};

<NotificationsVariantGroup />
</Tabs.Tab>

<Tabs.Tab>
```jsx copy
import { VKNotifications, useVKNotifications, Button } from "@vivakits/react-components";

const App = () => {
  const { notify } = useVKNotifications();

    return (
      <>
        <VKNotifications />
          <div className="flex items-center gap-3">
            <Button
              size="md"
              rounded="lg"
              onClick={() => {
                notify({
                  variant: "default",
                    title: "Default",
                    description: "Default Notification",
                    autoClose: true,
                    duration: 3000
                });
              }}
            >
              Default
            </Button>
            <Button
              size="md"
              rounded="lg"
              onClick={() => {
                notify({
                    variant: "success",
                    title: "Success",
                    description: "Success Notification",
                    autoClose: true,
                    duration: 3000,
                });
              }}
            >
              Success
            </Button>
            <Button
              size="md"
              rounded="lg"
              onClick={() => {
                notify({
                    variant: "warning",
                    title: "Warning",
                    description: "Warning Notification",
                    autoClose: true,
                    duration: 3000,
                });
              }}
            >
              Warning
            </Button>
            <Button
              size="md"
              rounded="lg"
              onClick={() => {
                notify({
                    variant: "error",
                    title: "Error",
                    description: "Error Notification",
                    autoClose: true,
                    duration: 3000,
                });
              }}
            >
              Error
          </Button>
      </div>
    </>
  );
};

export default App;
```
</Tabs.Tab>
</Tabs>

### Color

The `color` prop overrides the default color of the notification. This can be used to apply a custom color that matches the application's design or to highlight specific notifications.The Notification component comes with 8 different colors that you can change using the `color` prop inside the `notify` function.

<Tabs items={['Preview', 'Code']}>

<Tabs.Tab>
export const NotificationsColorGroup = () => {
    const { notify } = useVKNotifications();
  return (
    <>
        <VKNotifications />
        <div className="flex flex-wrap items-center gap-3">
            <Button
                size="md"
                rounded="lg"
                variant="outline"
                onClick={() => {
                    notify({
                        color: "default",
                        title: "Default",
                        description: "Default Notification",
                        autoClose: true,
                        duration: 3000
                    });
                }}
            >
                Default
            </Button>
            <Button
                size="md"
                rounded="lg"
                variant="outline"
                onClick={() => {
                    notify({
                        color: "primary",
                        title: "Primary",
                        description: "Primary Notification",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
                Primary
            </Button>
            <Button
                size="md"
                rounded="lg"
                variant="outline"
                color="secondary"
                onClick={() => {
                    notify({
                        color: "secondary",
                        title: "Secondary",
                        description: "Secondary Notification",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
                Secondary
            </Button>
            <Button
                size="md"
                rounded="lg"
                variant="outline"
                color="success"
                onClick={() => {
                    notify({
                        color: "success",
                        title: "Success",
                        description: "Success Notification",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
                Success
            </Button>
            <Button
                size="md"
                rounded="lg"
                variant="outline"
                color="danger"
                onClick={() => {
                    notify({
                        color: "danger",
                        title: "Danger",
                        description: "Danger Notification",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
                Danger
            </Button>
            <Button
                size="md"
                rounded="lg"
                variant="outline"
                color="warning"
                onClick={() => {
                    notify({
                        color: "warning",
                        title: "Warning",
                        description: "Warning Notification",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
                Warning
            </Button>
            <Button
                size="md"
                rounded="lg"
                variant="outline"
                color="dark"
                onClick={() => {
                    notify({
                        color: "dark",
                        title: "Dark",
                        description: "Dark Notification",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
                Dark
            </Button>
            <Button
                size="md"
                rounded="lg"
                variant="outline"
                color="light"
                onClick={() => {
                    notify({
                        color: "light",
                        title: "Light",
                        description: "Light Notification",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
                Light
            </Button>
        </div>
    </>

);
};

<NotificationsColorGroup />
</Tabs.Tab>

<Tabs.Tab>
```jsx copy
import { VKNotifications, useVKNotifications, Button } from "@vivakits/react-components";

const App = () => {
  const { notify } = useVKNotifications();

  return (
    <>
      <VKNotifications />
        <div className="flex flex-wrap items-center gap-3">
          <Button
            size="md"
            rounded="lg"
            variant="outline"
            onClick={() => {
              notify({
                  color: "default",
                  title: "Default",
                  description: "Default Notification",
                  autoClose: true,
                  duration: 3000
              });
            }}
          >
            Default
          </Button>
          <Button
            size="md"
            rounded="lg"
            variant="outline"
            onClick={() => {
              notify({
                  color: "primary",
                  title: "Primary",
                  description: "Primary Notification",
                  autoClose: true,
                  duration: 3000,
              });
            }}
          >
            Primary
          </Button>
          <Button
            size="md"
            rounded="lg"
            variant="outline"
            color="secondary"
            onClick={() => {
              notify({
                  color: "secondary",
                  title: "Secondary",
                  description: "Secondary Notification",
                  autoClose: true,
                  duration: 3000,
              });
            }}
          >
            Secondary
          </Button>
          <Button
            size="md"
            rounded="lg"
            variant="outline"
            color="success"
            onClick={() => {
              notify({
                  color: "success",
                  title: "Success",
                  description: "Success Notification",
                  autoClose: true,
                  duration: 3000,
              });
            }}
          >
            Success
          </Button>
          <Button
            size="md"
            rounded="lg"
            variant="outline"
            color="danger"
            onClick={() => {
              notify({
                  color: "danger",
                  title: "Danger",
                  description: "Danger Notification",
                  autoClose: true,
                  duration: 3000,
              });
            }}
          >
            Danger
          </Button>
          <Button
            size="md"
            rounded="lg"
            variant="outline"
            color="warning"
            onClick={() => {
              notify({
                  color: "warning",
                  title: "Warning",
                  description: "Warning Notification",
                  autoClose: true,
                  duration: 3000,
              });
            }}
          >
            Warning
          </Button>
          <Button
            size="md"
            rounded="lg"
            variant="outline"
            color="dark"
            onClick={() => {
              notify({
                  color: "dark",
                  title: "Dark",
                  description: "Dark Notification",
                  autoClose: true,
                  duration: 3000,
              });
            }}
          >
            Dark
          </Button>
          <Button
            size="md"
            rounded="lg"
            variant="outline"
            color="light"
            onClick={() => {
              notify({
                  color: "light",
                  title: "Light",
                  description: "Light Notification",
                  autoClose: true,
                  duration: 3000,
              });
            }}
          >
            Light
          </Button>
      </div>
    </>
  );
};

export default App;
```

</Tabs.Tab>
</Tabs>

### Size

The `size` prop determines the size of the notification. Options include 6 different sizes allowing the notification to fit different spaces within the application layout. The component size can be changed using the `size` prop inside the `notify` function.

<Tabs items={['Preview', 'Code']}>

<Tabs.Tab>
export const NotificationsSizeGroup = () => {
    const { notify } = useVKNotifications();
  return (
    <>
        <VKNotifications />
        <div className="flex items-center gap-3">
            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        size: "xs",
                        title: "Extra small",
                        description: "Extra small Notification",
                        autoClose: true,
                        duration: 3000
                    });
                }}
            >
                Extra small
            </Button>
            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        size: "sm",
                        title: "Small",
                        description: "Small Notification",
                        autoClose: true,
                        duration: 3000
                    });
                }}
            >
                Small
            </Button>
            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        size: "md",
                        title: "Medium",
                        description: "Medium Notification",
                        autoClose: true,
                        duration: 3000
                    });
                }}
            >
                Medium
            </Button>
            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        size: "lg",
                        title: "Large",
                        description: "Large Notification",
                        autoClose: true,
                        duration: 3000
                    });
                }}
            >
                Large
            </Button>
            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        size: "xl",
                        title: "Extra large",
                        description: "Extra large Notification",
                        autoClose: true,
                        duration: 3000
                    });
                }}
            >
                Extra large
            </Button>
            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        size: "2xl",
                        title: "Double extra large",
                        description: "Double extra large Notification",
                        autoClose: true,
                        duration: 3000
                    });
                }}
            >
                Double extra large
            </Button>
        </div>
    </>

);
};

<NotificationsSizeGroup />
</Tabs.Tab>

<Tabs.Tab>
```jsx copy
import { VKNotifications, useVKNotifications, Button } from "@vivakits/react-components";

const App = () => {
  const { notify } = useVKNotifications();
  return (
    <>
      <VKNotifications />
        <div className="flex items-center gap-3">
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  size: "xs",
                  title: "Extra small",
                  description: "Extra small Notification",
                  autoClose: true,
                  duration: 3000
              });
            }}
          >
            Extra small
          </Button>
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  size: "sm",
                  title: "Small",
                  description: "Small Notification",
                  autoClose: true,
                  duration: 3000
              });
            }}
          >
            Small
          </Button>
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  size: "md",
                  title: "Medium",
                  description: "Medium Notification",
                  autoClose: true,
                  duration: 3000
              });
            }}
          >
            Medium
          </Button>
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  size: "lg",
                  title: "Large",
                  description: "Large Notification",
                  autoClose: true,
                  duration: 3000
              });
            }}
          >
            Large
          </Button>
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  size: "xl",
                  title: "Extra large",
                  description: "Extra large Notification",
                  autoClose: true,
                  duration: 3000
              });
            }}
          >
            Extra large
          </Button>
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  size: "2xl",
                  title: "Double extra large",
                  description: "Double extra large Notification",
                  autoClose: true,
                  duration: 3000
              });
            }}
          >
            Double extra large
        </Button>
      </div>
    </>
  );
};

export default App;
```

</Tabs.Tab>
</Tabs>



### Placement

Specifies where on the screen the notification should appear. Common placements include `top-right` `top-left` `bottom-right` and `bottom-left`. This allows notifications to be shown in the most appropriate location based on the context. Notification component comes with 4 different placements that can be changed using the `placement` prop inside the `notify` function.

<Tabs items={['Preview', 'Code']}>

<Tabs.Tab>
export const NotificationsPlacementGroup = () => {
    const { notify } = useVKNotifications();
  return (
    <>
        <VKNotifications />
        <div className="flex items-center gap-3">
            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        title: "Top Left",
                        description: "Top Left Notification",
                        autoClose: true,
                        duration: 3000,
                        placement: "topLeft"
                    });
                }}
            >
                Top Left
            </Button>
            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        title: "Top Right",
                        description: "Top Right Notification",
                        autoClose: true,
                        duration: 3000,
                        placement: "topRight"
                    });
                }}
            >
                Top Right
            </Button>
            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        title: "Bottom Left",
                        description: "Bottom Left Notification",
                        autoClose: true,
                        duration: 3000,
                        placement: "bottomLeft"
                    });
                }}
            >
                Bottom Left
            </Button>
            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        title: "Bottom Right",
                        description: "Bottom Right Notification",
                        autoClose: true,
                        duration: 3000,
                        placement: "bottomRight"
                    });
                }}
            >
                Bottom Right
            </Button>
        </div>
    </>

);
};

<NotificationsPlacementGroup />
</Tabs.Tab>

<Tabs.Tab>
```jsx copy
import { VKNotifications, useVKNotifications, Button } from "@vivakits/react-components";

const App = () => {
  const { notify } = useVKNotifications();
  return (
    <>
      <VKNotifications />
        <div className="flex items-center gap-3">
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  title: "Top Left",
                  description: "Top Left Notification",
                  autoClose: true,
                  duration: 3000,
                  placement: "topLeft"
              });
            }}
          >
            Top Left
          </Button>
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  title: "Top Right",
                  description: "Top Right Notification",
                  autoClose: true,
                  duration: 3000,
                  placement: "topRight"
              });
            }}
          >
            Top Right
          </Button>
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  title: "Bottom Left",
                  description: "Bottom Left Notification",
                  autoClose: true,
                  duration: 3000,
                  placement: "bottomLeft"
              });
            }}
          >
            Bottom Left
          </Button>
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  title: "Bottom Right",
                  description: "Bottom Right Notification",
                  autoClose: true,
                  duration: 3000,
                  placement: "bottomRight"
              });
            }}
          >
            Bottom Right
          </Button>
      </div>
    </>
  );
};

export default App;
```
</Tabs.Tab>
</Tabs>

### Font family

The `fontFamily` prop overrides the default font family for the notification text. This can ensure the notification matches the typography of the rest of the application that can be changed using the `variant` prop inside the `notify` function.

<Tabs items={['Preview', 'Code']}>

<Tabs.Tab>
export const NotificationFontFamily = () => {
    const { notify } = useVKNotifications();
  return (
    <>
        <VKNotifications />
        <div className="flex items-center gap-3">
            <Button
                size="md"
                rounded="lg"
               onClick={() => {
                    notify({
                     fontFamily:"default",
                        title: "Default",
                        description: "Default font family",
                        autoClose: true,
                        duration: 3000
                    });
                }}
            >
                Default
            </Button>

            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        fontFamily:"mono",
                        title: "Mono",
                        description: "Mono font family",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
                Mono
            </Button>

            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        fontFamily:"sansSerif",
                        title: "Sans Serif",
                        description: "Sans Serif font family",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
                Sans Serif
            </Button>

            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        fontFamily:"serif",
                        title: "Serif",
                        description: "Sans Serif font family",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
                Serif
            </Button>
        </div>
    </>

);
};

<NotificationFontFamily />
</Tabs.Tab>

<Tabs.Tab>
```jsx copy
import { VKNotifications, useVKNotifications, Button } from "@vivakits/react-components";

const App = () => {
  const { notify } = useVKNotifications();

  return (
    <>
      <VKNotifications />
        <div className="flex items-center gap-3">
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                fontFamily:"default",
                  title: "Default",
                  description: "Default font family",
                  autoClose: true,
                  duration: 3000
              });
            }}
          >
            Default
          </Button>
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  fontFamily:"mono",
                  title: "Mono",
                  description: "Mono font family",
                  autoClose: true,
                  duration: 3000,
              });
            }}
          >
            Mono
          </Button>
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  fontFamily:"sansSerif",
                  title: "Sans Serif",
                  description: "Sans Serif font family",
                  autoClose: true,
                  duration: 3000,
              });
            }}
          >
            Sans Serif
          </Button>
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  fontFamily:"serif",
                  title: "Serif",
                  description: "Sans Serif font family",
                  autoClose: true,
                  duration: 3000,
              });
            }}
          >
            Serif
          </Button>
      </div>
    </>
  );
};

export default App;
```
</Tabs.Tab>
</Tabs>

### Close notification

The `autoClose` prop determines whether the notification should automatically close after a certain duration. If set to `false`, the notification will remain visible until manually closed. If `autoClose` is set to `true`, the duration for the notification to be visible can be controlled using the `duration` prop. The `duration` is in millisecond.

<Tabs items={['Preview', 'Code']}>

<Tabs.Tab>
export const NotificationClose = () => {
    const { notify } = useVKNotifications();
  return (
    <>
        <VKNotifications />
        <div className="flex items-center gap-3">
            <Button
                size="md"
                rounded="lg"
               onClick={() => {
                    notify({
                        title: "Auto close",
                        description: "Auto close notification",
                        autoClose: true,
                        duration: 3000
                    });
                }}
            >
                Auto close
            </Button>
            <Button
            size="md"
            rounded="lg"
            onClick={() => {
                notify({
                  title: "Without auto close",
                  description: "Without auto close notification",
                  autoClose: false,
              });
            }}
          >
            Without auto close
          </Button>
          </div>
    </>

);
};

<NotificationClose />
</Tabs.Tab>

<Tabs.Tab>
```jsx copy
import { VKNotifications, useVKNotifications, Button } from "@vivakits/react-components";

const App = () => {
  const { notify } = useVKNotifications();

  return (
    <>
      <VKNotifications />
        <div className="flex items-center gap-3">
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  title: "Auto close",
                  description: "Auto close notification",
                  autoClose: true,
                  duration: 3000
              });
            }}
          >
            Auto close
          </Button>
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
                notify({
                  title: "Without auto close",
                  description: "Without auto close notification",
                  autoClose: false,
              });
            }}
          >
            Without auto close
          </Button>
        </div>
    </>
  );
};

export default App;
```
</Tabs.Tab>
</Tabs>

### Example

Below are examples of how notification can be used in various circumstances.

<Tabs items={['Preview', 'Code']}>

<Tabs.Tab>
export const NotificationEample = () => {
    const { notify } = useVKNotifications();
  return (
    <>
        <VKNotifications />
        <div className="flex items-center gap-3">
            <Button
                size="md"
                rounded="lg"
                onClick={() => {
                    notify({
                        variant:"success",
                        title: "Success",
                        description: "Your meeting has been attended successfully.",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
              Attend meeting
            </Button>
            <Button
                size="md"
                rounded="lg"
                color="danger"
                onClick={() => {
                    notify({
                        variant:"error",
                        title: "Error",
                        description: "Document has been permanently deleted",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
              Delete
            </Button>
            <Button
                size="md"
                rounded="lg"
                color="warning"
                onClick={() => {
                    notify({
                        size:"xl",
                        variant:"warning",
                        title: "Warning",
                        description: "Your local date time is not is sync with the server's current date",
                        autoClose: true,
                        duration: 3000,
                    });
                }}
            >
              Date and time
            </Button>
        </div>
    </>

);
};

<NotificationEample />
</Tabs.Tab>

<Tabs.Tab>
```jsx copy
import { VKNotifications, useVKNotifications, Button } from "@vivakits/react-components";

const App = () => {
  const { notify } = useVKNotifications();
  return (
    <>
      <VKNotifications />
        <div className="flex items-center gap-3">
          <Button
            size="md"
            rounded="lg"
            onClick={() => {
              notify({
                  variant:"success",
                  title: "Success",
                  description: "Your meeting has been attended successfully.",
                  autoClose: true,
                  duration: 3000,
              });
            }}
          >
            Attend meeting
          </Button>
          <Button
            size="md"
            rounded="lg"
            color="danger"
            onClick={() => {
              notify({
                  variant:"error",
                  title: "Error",
                  description: "Document has been permanently deleted",
                  autoClose: true,
                  duration: 3000,
              });
            }}
          >
            Delete
          </Button>
          <Button
            size="md"
            rounded="lg"
            color="warning"
            onClick={() => {
              notify({
                  size:"xl",
                  variant:"warning",
                  title: "Warning",
                  description: "Your local date time is not is sync with the server's current date",
                  autoClose: true,
                  duration: 3000,
              });
            }}
          >
            Date and time
          </Button>
        </div>
    </>
  );
};

export default App;
```
</Tabs.Tab>
</Tabs>

### API Reference

{<table>

  <thead>
    <tr>
      <th className="w-[10%]" align="left">Attribute</th>
      <th className="w-[50%]" align="left">Type</th>
      <th className="w-[30%]" align="left">Description</th>
      <th className="w-[10%]" align="left">Default</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>variant</td>
      <td className="flex flex-wrap gap-y-2 border-none">
        <span className="badge">default</span>
        <span className="badge">success</span>
        <span className="badge">warning</span>
        <span className="badge">error</span>
      </td>
      <td>Changes notification variant</td>
      <td><span className="badge">default</span></td>
    </tr>
    <tr>
      <td>size</td>
      <td className="flex flex-wrap gap-y-2 border-none">
        <span className="badge">xs</span>
        <span className="badge">sm</span>
        <span className="badge">md</span>
        <span className="badge">lg</span>
        <span className="badge">xl</span>
        <span className="badge">2xl</span>
      </td>
      <td>Changes size on notification</td>
      <td><span className="badge">md</span></td>
    </tr>
    <tr>
      <td>color</td>
      <td className="flex flex-wrap gap-y-2 border-none">
        <span className="badge">default</span>
        <span className="badge">primary</span>
        <span className="badge">secondary</span>
        <span className="badge">success</span>
        <span className="badge">danger</span>
        <span className="badge">warning</span>
        <span className="badge">dark</span>
        <span className="badge">light</span>
      </td>
      <td>Changes notification color</td>
      <td><span className="badge">default</span></td>
    </tr>
    <tr>
      <td>fontFamily</td>
      <td className="flex flex-wrap gap-y-2 border-none">
        <span className="badge">default</span>
        <span className="badge">sansSerif</span>
        <span className="badge">serif</span>
        <span className="badge">mono</span>
      </td>
      <td>Changes font family on notification text</td>
      <td><span className="badge">default</span></td>
    </tr>
    <tr>
      <td>placement</td>
      <td className="flex flex-wrap gap-y-2 border-none">
        <span className="badge">topLeft</span>
        <span className="badge">topRight</span>
        <span className="badge">bottomLeft</span>
        <span className="badge">bottomRight</span>
      </td>
      <td>Changes placement of notification</td>
      <td><span className="badge">topRight</span></td>
    </tr>
    <tr>
      <td>fontSize</td>
      <td>string</td>
      <td>Changes font size of the notification text</td>
      <td><span className="badge">sm</span></td>
    </tr>
     <tr>
      <td>autoClose</td>
      <td>
        boolean
      </td>
      <td>Determines notification auto closing</td>
      <td><span className="badge">true</span></td>
    </tr>
    <tr>
      <td>id</td>
      <td>string</td>
      <td>Unique identifier for the notification</td>
      <td>-</td>
    </tr>
    <tr>
      <td>icon</td>
      <td>ReactNode</td>
      <td>Icon displayed in the notification</td>
      <td>-</td>
    </tr>
    <tr>
      <td>closeIcon</td>
      <td>ReactNode</td>
      <td>Icon for the close button</td>
      <td>-</td>
    </tr>
    <tr>
      <td>title</td>
      <td>string</td>
      <td>Title of the notification</td>
      <td>-</td>
    </tr>
    <tr>
      <td>description</td>
      <td>string</td>
      <td>Description text of the notification</td>
      <td>-</td>
    </tr>
    <tr>
      <td>onClose</td>
      <td>function</td>
      <td>Callback function when the notification is closed</td>
      <td>-</td>
    </tr>
      <tr>
      <td>hasMarginTop</td>
      <td>boolean</td>
      <td>Adds margin to the top of the notification</td>
      <td><span className="badge">false</span></td>
    </tr>
      <tr>
      <td>duration</td>
      <td>number</td>
      <td>Duration before the notification auto-closes (in ms)</td>
      <td><span className="badge">3000</span></td>
    </tr>
    <tr>
      <td>role</td>
      <td>string</td>
      <td>ARIA role for the notification</td>
      <td>-</td>
    </tr>
    <tr>
      <td>className</td>
      <td>string</td>
      <td>Additional class names for styling</td>
      <td>-</td>
    </tr>
    <tr>
      <td>props</td>
      <td>object</td>
      <td>Additional props passed to the notification</td>
      <td>-</td>
    </tr>
  </tbody>
</table>}
