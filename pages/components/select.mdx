import { Select } from "@vivakits/react-components";
import { Tabs } from "nextra/components";

# Select

### Variants

The Select component comes with 2 different variants that can be changed using the `variant` prop.

<Tabs items={['Preview', 'Code']}>

  <Tabs.Tab>
export const SelectVariantGroup = () => {
  return (
    <div className="flex items-center gap-3">
<Select  />
    </div>

);
};

<SelectVariantGroup />
  </Tabs.Tab>

  <Tabs.Tab>
```jsx copy
    <div className="flex items-center gap-3">
        
    </div>
```
  </Tabs.Tab>
</Tabs>
