import { Tabs } from "nextra/components";
import Image from "next/image";
import {LoginForm} from "./allexamples.tsx";

import {
  Modal,
  ModalBody,
  ModalContent,
  ModalFooter,
  ModalHeader,
  ModalTrigger,
} from "@vivakits/react-components";

# Layout Example

<div className="mt-10 grid grid-cols-1 sm:grid-cols-2 gap-5">
<Modal varient="screen-size">
    <div className="flex flex-col items-start gap-2 p-2 rounded-lg bg-slate-100">
        <LoginForm/>
        <ModalTrigger>
            <div className="my-4 flex items-center gap-2 cursor-pointer transition-all duration-200 hover:translate-x-2">
                <h1>Login Form</h1>
                <Image src="/right-arrow.svg" width={25} height={25} alt="Vivakits" />
            </div>
        </ModalTrigger>
    </div>
    <ModalContent className="w-[90%] h-full">
        <ModalHeader className="px-4">Basic Login</ModalHeader>
            <ModalBody className="p-4">
                <Tabs items={['Preview', 'Code']}>
                    <Tabs.Tab>
                        <div className="w-1/2 mx-auto">
                            <LoginForm/>
                        </div>
                    </Tabs.Tab>
                    <Tabs.Tab>
```jsx copy
import { Input, Button, Checkbox } from "@vivakits/react-components";

const LoginForm = ()=>{
    return(
        <div className="w-full flex flex-col gap-2 bg-slate-50 p-4 rounded-lg mx-auto my-0">
            <Input size="sm" label="User name" type="text" />
            <Input size="sm" label="Password" type="password" />
            <Button size="md" rounded="md" className="w-fit mx-auto">Login</Button>
            <Checkbox size="md">Remember Password</Checkbox>
            <a href="#" className="text-blue-800">Forgot your password?</a>
            <p className="m-0">click <a href="#" className="text-blue-800">here</a> to reset your password.</p>
        </div>
    )
}
```
                    </Tabs.Tab>
                </Tabs>
            </ModalBody>
        </ModalContent>
    </Modal>
</div>
